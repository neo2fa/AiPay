## Packages
recharts | Dashboard analytics visualization
framer-motion | Smooth page transitions and micro-interactions
date-fns | Date formatting for invoices
clsx | Class name utility
tailwind-merge | Class name merging

## Notes
- Images should be handled via simulated uploads (returning mock URLs) for the prototype
- Auth is handled via Replit Auth
- Font selection: Inter (sans) + Outfit (display)
